-- Khởi tạo Window như bình thường
local AuroraLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/Giacchsbloxfruit/Giacyeuem/refs/heads/main/testUI"))()
local Window = AuroraLib:CreateWindow({
    Name = "Giacyeuem Hub",
    SubName = "ver 1.0 by Giacdz Hub"
})

--- Logo
local CoreGui = game:GetService("CoreGui")
local UserInputService = game:GetService("UserInputService")

-- Đảm bảo xóa nút cũ nếu script được chạy lại nhiều lần
if CoreGui:FindFirstChild("AuroraLogoGui") then
    CoreGui.AuroraLogoGui:Destroy()
end

local LogoGui = Instance.new("ScreenGui", CoreGui)
LogoGui.Name = "AuroraLogoGui"

-- [[ 1. TẠO NÚT LOGO (VIỀN VUÔNG BO GÓC) ]]
local LogoButton = Instance.new("ImageButton", LogoGui)
LogoButton.Size = UDim2.new(0, 50, 0, 50) -- Kích thước vuông 50x50
LogoButton.Position = UDim2.new(0.1, 0, 0.1, 0) -- Vị trí mặc định
LogoButton.BackgroundColor3 = Color3.fromRGB(20, 24, 28) -- Màu nền nút
LogoButton.Image = "rbxassetid://89781006721784" -- ĐIỀN ID ẢNH LOGO CỦA ÔNG VÀO ĐÂY (VD: rbxassetid://123456789)
LogoButton.ClipsDescendants = true

-- [[ 2. BO GÓC ]]
local UICorner = Instance.new("UICorner", LogoButton)
UICorner.CornerRadius = UDim.new(0, 10) -- Độ bo góc (thay đổi số 10 nếu muốn bo nhiều/ít hơn)

-- [[ 3. VIỀN NGOÀI (STROKE) ]]
local UIStroke = Instance.new("UIStroke", LogoButton)
UIStroke.Color = Color3.fromRGB(255, 255, 255) -- Màu viền (Mặc định trắng)
UIStroke.Thickness = 1.5 -- Độ dày viền
UIStroke.Transparency = 0.4 -- Độ mờ của viền

-- [[ 4. CHỨC NĂNG KÉO THẢ (DRAGGABLE) ]]
local dragging, dragInput, dragStart, startPos
LogoButton.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = LogoButton.Position
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        LogoButton.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)

-- [[ 5. CHỨC NĂNG CLICK ĐỂ BẬT/TẮT UI CHÍNH ]]
LogoButton.MouseButton1Click:Connect(function()
    local mainUI = CoreGui:FindFirstChild("AuroraUI")
    if mainUI then
        mainUI.Enabled = not mainUI.Enabled -- Bật/Tắt hiển thị của ScreenGui chính
    end
end)

AuroraLib:Notification({
    Title = "Giacyeuem Hub",
    Text = "Welcome",
    Duration = 5 -- Thời gian hiển thị (giây)
})
-- Tạo một Tab Cài Đặt riêng
local Tabs = {
    Info = Window:MakeTab({ Name = "Tab Information"}),
    Main = Window:MakeTab({ Name = "Farming"}),
    Sea = Window:MakeTab({ Name = "Tab Sea Event"}),
    Other = Window:MakeTab({ Name = "Tab Farming Other"}),
    Setting = Window:MakeTab({ Name = "Setting Farm"}),
    Player = Window:MakeTab({ Name = "Tab Local Player"}),
    Teleport = Window:MakeTab({ Name = "Tab Teleport"}),
    Visual = Window:MakeTab({ Name = "Tab Visual"}),
    Fruit = Window:MakeTab({ Name = "Tab Fruit And Raid"}),
    Race = Window:MakeTab({ Name = "Tab Upgrade Race"}),
    Shop = Window:MakeTab({ Name = "Tab Shop"}),
};
-- Hàm server
local function RejoinServer()
    AuroraLib:Notification({Title = "Success", Text = "Rejoining...", Duration = 3})
    task.spawn(function()
        pcall(function()
            game:GetService("TeleportService"):TeleportToPlaceInstance(game.PlaceId, game.JobId, game.Players.LocalPlayer)
        end)
    end)
end

local function HopServer()
    AuroraLib:Notification({Title = "Processing", Text = "Hopping...", Duration = 2})
    task.spawn(function()
        pcall(function()
            game:GetService("ReplicatedStorage").__ServerBrowser:InvokeServer("hop")
        end)
        task.wait(1)
        pcall(function()
            game:GetService("TeleportService"):Teleport(game.PlaceId, game.Players.LocalPlayer)
        end)
    end)
end

Tabs.Setting:AddToggle({
    Name = "Tự động Gom Quái",
    Callback = function(state)
        print("Trạng thái:", state)
    end
})
Tabs.Setting:AddSection({Name = "Tùy Biến Giao Diện"})
Tabs.Setting:AddButton({Name = "Tùy Biến Giao Diện"})
-- Tạo Dropdown chọn màu theo danh sách yêu cầu
Tabs.Info:AddParagraph({
    Name = "Thanks for using our script",
    Desc = "If you encounter any errors, please send us your feedback"
})
Tabs.Info:AddSection({Name = "Social media" })
Tabs.Info:AddButton({
    Name = "Copy Link Discord",
    Callback = function()
        local copyFunc = setclipboard or toclipboard
        if copyFunc then
            copyFunc("https://discord.gg/abc") -- Thay link vào đây
            AuroraLib:Notification({Title = "Thành Công", Text = "Đã copy link Discord!", Duration = 3})
        else
            AuroraLib:Notification({Title = "Thất Bại", Text = "Executor không hỗ trợ!", Duration = 3})
        end
    end
})
Tabs.Info:AddSection({ Name = "Check Stock Fruits" })

local function FormatPrice(priceValue)
    local formatted = tostring(priceValue)
    repeat
        local k
        formatted, k = string.gsub(formatted, "^(-?%d+)(%d%d%d)", "%1,%2")
    until k == 0
    return formatted
end

local CommF = game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("CommF_")

-- Hàm lấy chuỗi danh sách bằng tiếng Anh, viết hoa tiêu đề
local function GetFruitStockString()
    local stockText = "ADVANCE FRUIT STOCK\n"
    
    local status1, result1 = pcall(function()
        return CommF:InvokeServer("GetFruits", true)
    end)
    
    if not status1 or not result1 then
        stockText = stockText .. "  Error fetching advance stock data.\n"
    else
        local hasAdvance = false
        for _, fruit in pairs(result1) do
            if fruit.OnSale then
                hasAdvance = true
                stockText = stockText .. "  • " .. tostring(fruit.Name) .. " — $" .. FormatPrice(fruit.Price) .. "\n"
            end
        end
        if not hasAdvance then
            stockText = stockText .. "  No fruits currently on sale.\n"
        end
    end
    
    stockText = stockText .. "\nNORMAL FRUIT STOCK\n"
    
    local status2, result2 = pcall(function()
        return CommF:InvokeServer("GetFruits")
    end)
    
    if status2 and result2 then
        local hasNormal = false
        for _, fruit in pairs(result2) do
            if fruit.OnSale then
                hasNormal = true
                stockText = stockText .. "  • " .. tostring(fruit.Name) .. " — $" .. FormatPrice(fruit.Price) .. "\n"
            end
        end
        if not hasNormal then
            stockText = stockText .. "  No fruits currently on sale.\n"
        end
    else
        stockText = stockText .. "  Error fetching normal stock data.\n"
    end
    
    return stockText
end

-- 1. Tạo Khung Paragraph và lưu vào biến (Lúc này Core trả về ElementActions có hàm :Set)
local StockParagraph = Tabs.Info:AddParagraph({
    Name = "Fruit Stock Status",
    Desc = "Fetching data from server, please wait..."
})

-- Hàm gọi cập nhật ngầm không lo treo UI bên ngoài
local function UpdateStock()
    task.spawn(function()
        local latestStock = GetFruitStockString()
        -- Gọi thẳng hàm :Set() vừa viết trong Core
        if StockParagraph and StockParagraph.Set then
            StockParagraph:Set(latestStock)
        end
    end)
end

-- 2. Tạo nút bấm làm mới thủ công
Tabs.Info:AddButton({
    Name = "Refresh Fruit Stock",
    Desc = "Click to instantly update the fruit shop status",
    Callback = function()
        UpdateStock()
        AuroraLib:Notification({
            Title = "System",
            Text = "Fruit stock updated successfully!",
            Duration = 2
        })
    end
})

-- Chạy cập nhật lần đầu tiên
UpdateStock()

-- Vòng lặp chạy ngầm tự động làm mới mỗi 60 giây
task.spawn(function()
    while task.wait(60) do
        UpdateStock()
    end
end)

Tabs.Info:AddSection({ Name = "Server"})
Tabs.Info:AddButton({
    Name = "Rejoin Server",
    Callback = RejoinServer
})

Tabs.Info:AddButton({
    Name = "Server Hop",
    Callback = HopServer
})
-- 1. NÚT COPY JOBID HIỆN TẠI
Tabs.Info:AddButton({
    Name = "Copy JobID",
    Callback = function()
        local copyFunc = setclipboard or toclipboard
        if copyFunc then
            copyFunc(tostring(game.JobId))
            AuroraLib:Notification({Title = "Success", Text = "Copied to clipboard!", Duration = 3})
        else
            AuroraLib:Notification({Title = "Failed", Text = "Copy failed!", Duration = 3})
        end
    end
})

-- 2. Ô NHẬP JOBID ĐỂ DỊCH CHUYỂN
Tabs.Info:AddInput({
    Name = "Join Server by JobID",
    Placeholder = "Paste JobID and enter",
    Callback = function(text)
        if text and text ~= "" then
            AuroraLib:Notification({Title = "Waiting", Text = "Hopping to " .. text, Duration = 3})
            pcall(function()
                -- Lệnh dịch chuyển theo yêu cầu của bạn
                game:GetService("ReplicatedStorage").__ServerBrowser:InvokeServer("teleport", text)
            end)
        end
    end
})

Tabs.Visual:AddDropdown({
    Name = "Change theme color",
    Desc = "Chọn màu sắc cho Giacyeuem Hub",
    Options = {"White", "Purple", "Pink", "Red", "Blue", "Green", "Orange", "Yellow", "Rainbow"},
    Default = "White",
    Callback = function(SelectedColor)
        -- Gọi thẳng hàm SetAccent có sẵn trong hình của ông
        Window:SetAccent(SelectedColor)
    end
})

Tabs.Setting:AddSection({ Name = "Tùy Biến Giao Diện" })

