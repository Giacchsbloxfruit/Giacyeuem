local Axiom = {}
Axiom.__index = Axiom

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")

local LocalPlayer = Players.LocalPlayer

local Theme = {
	Background = Color3.fromRGB(5, 5, 7),
	Sidebar = Color3.fromRGB(7, 7, 9),
	Card = Color3.fromRGB(12, 12, 15),
	CardHover = Color3.fromRGB(18, 18, 21),
	Text = Color3.fromRGB(240, 240, 244),
	SubText = Color3.fromRGB(145, 145, 153),
	Muted = Color3.fromRGB(88, 88, 96),
	White = Color3.fromRGB(248, 248, 250),
	Black = Color3.fromRGB(2, 2, 3),
	Border = Color3.fromRGB(48, 48, 54),
	BorderLight = Color3.fromRGB(70, 70, 78),
	Success = Color3.fromRGB(90, 255, 145),
	Danger = Color3.fromRGB(255, 85, 85),
	Warning = Color3.fromRGB(255, 200, 80),
	Neon = Color3.fromRGB(220, 220, 255)
}

local WINDOW_WIDTH = 900
local WINDOW_HEIGHT = 560
local SIDEBAR_WIDTH = 214
local TOPBAR_HEIGHT = 54

local TAB_HEIGHT = 36
local TAB_GAP = 3

local SECTION_GAP = 8
local CONTROL_GAP = 5

local function Create(className, properties)
	local object = Instance.new(className)

	for property, value in pairs(properties or {}) do
		object[property] = value
	end

	return object
end

local function Tween(object, properties, duration)
	local animation = TweenService:Create(
		object,
		TweenInfo.new(
			duration or 0.16,
			Enum.EasingStyle.Quint,
			Enum.EasingDirection.Out
		),
		properties
	)

	animation:Play()

	return animation
end

local function Corner(object, radius)
	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, radius or 8)
	corner.Parent = object
	return corner
end

local function Stroke(object, color, transparency, thickness)
	local stroke = Instance.new("UIStroke")

	stroke.Color = color or Theme.Border
	stroke.Transparency = transparency or 0
	stroke.Thickness = thickness or 1
	stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

	stroke.Parent = object

	return stroke
end

local function Label(parent, text, size, color, font)
	return Create("TextLabel", {
		Parent = parent,
		BackgroundTransparency = 1,
		Text = text or "",
		TextSize = size or 13,
		TextColor3 = color or Theme.Text,
		Font = font or Enum.Font.Gotham,
		TextXAlignment = Enum.TextXAlignment.Left,
		TextYAlignment = Enum.TextYAlignment.Center,
		RichText = true
	})
end

local function Button(parent)
	return Create("TextButton", {
		Parent = parent,
		BackgroundTransparency = 0,
		BackgroundColor3 = Theme.Card,
		Text = "",
		AutoButtonColor = false,
		BorderSizePixel = 0
	})
end

local function TransparentButton(parent)
	return Create("TextButton", {
		Parent = parent,
		BackgroundTransparency = 1,
		Text = "",
		AutoButtonColor = false,
		BorderSizePixel = 0
	})
end

local function MakeList(parent, padding)
	return Create("UIListLayout", {
		Parent = parent,
		FillDirection = Enum.FillDirection.Vertical,
		HorizontalAlignment = Enum.HorizontalAlignment.Center,
		VerticalAlignment = Enum.VerticalAlignment.Top,
		Padding = UDim.new(0, padding or 5),
		SortOrder = Enum.SortOrder.LayoutOrder
	})
end

local function Callback(callback, ...)
	if typeof(callback) == "function" then
		task.spawn(callback, ...)
	end
end

function Axiom:CreateWindow(options)
	options = options or {}

	local Window = {
		Tabs = {},
		TabOrder = {},
		CurrentTab = nil,
		Visible = true,
		OrbIndex = 0
	}

	local ScreenGui = Create("ScreenGui", {
		Name = "AxiomUI",
		Parent = LocalPlayer:WaitForChild("PlayerGui"),
		IgnoreGuiInset = true,
		ResetOnSpawn = false,
		ZIndexBehavior = Enum.ZIndexBehavior.Sibling
	})

	Window.ScreenGui = ScreenGui

	local Shell = Create("Frame", {
		Parent = ScreenGui,
		AnchorPoint = Vector2.new(0.5, 0.5),
		Position = UDim2.fromScale(0.5, 0.5),
		Size = UDim2.fromOffset(WINDOW_WIDTH, WINDOW_HEIGHT),
		BackgroundColor3 = Theme.Background,
		BorderSizePixel = 0
	})

	Corner(Shell, 15)
	Stroke(Shell, Theme.White, 0.72, 1)

	Window.Shell = Shell

	local Background = Create("Frame", {
		Parent = Shell,
		Position = UDim2.fromOffset(10, 10),
		Size = UDim2.new(1, -20, 1, -20),
		BackgroundColor3 = Theme.Background,
		BorderSizePixel = 0,
		ClipsDescendants = true,
		ZIndex = 1
	})

	Corner(Background, 11)

	local Gradient = Create("UIGradient", {
		Parent = Background,
		Rotation = 90,
		Color = ColorSequence.new({
			ColorSequenceKeypoint.new(0, Color3.fromRGB(10, 10, 13)),
			ColorSequenceKeypoint.new(0.5, Color3.fromRGB(6, 6, 8)),
			ColorSequenceKeypoint.new(1, Color3.fromRGB(4, 4, 6))
		})
	})

	local BackgroundLayer = Create("Frame", {
		Parent = Background,
		Position = UDim2.fromOffset(2, 2),
		Size = UDim2.new(1, -4, 1, -4),
		BackgroundTransparency = 1,
		ClipsDescendants = true,
		ZIndex = 2
	})

	Corner(BackgroundLayer, 10)

	local Orb1 = Create("Frame", {
		Parent = BackgroundLayer,
		Size = UDim2.fromOffset(225, 225),
		BackgroundColor3 = Theme.Neon,
		BackgroundTransparency = 0.979,
		BorderSizePixel = 0,
		ZIndex = 2
	})

	Corner(Orb1, 225)
	Stroke(Orb1, Theme.Neon, 0.95, 2)

	local Orb2 = Create("Frame", {
		Parent = BackgroundLayer,
		Size = UDim2.fromOffset(195, 195),
		BackgroundColor3 = Theme.Neon,
		BackgroundTransparency = 0.98,
		BorderSizePixel = 0,
		ZIndex = 2
	})

	Corner(Orb2, 195)
	Stroke(Orb2, Theme.Neon, 0.96, 2)

	local DustLayer = Create("Frame", {
		Parent = BackgroundLayer,
		Size = UDim2.fromScale(1, 1),
		BackgroundTransparency = 1,
		ClipsDescendants = true,
		ZIndex = 3
	})

	math.randomseed(math.floor(os.clock() * 100000))

	for i = 1, 20 do
		local size = math.random(2, 4)

		local dust = Create("Frame", {
			Parent = DustLayer,
			Position = UDim2.fromScale(
				math.random(5, 95) / 100,
				math.random(5, 95) / 100
			),
			Size = UDim2.fromOffset(size, size),
			BackgroundColor3 = Theme.White,
			BackgroundTransparency = math.random(72, 91) / 100,
			BorderSizePixel = 0,
			ZIndex = 3
		})

		Corner(dust, size)

		task.spawn(function()
			while dust.Parent do
				local destination = UDim2.fromScale(
					math.random(5, 95) / 100,
					math.random(5, 95) / 100
				)

				Tween(dust, {
					Position = destination,
					BackgroundTransparency = math.random(70, 92) / 100
				}, math.random(18, 32) / 10)

				task.wait(math.random(18, 32) / 10)
			end
		end)
	end

	local orbPositions = {
		{
			UDim2.new(-0.12, 0, -0.12, 0),
			UDim2.new(0.82, 0, 0.73, 0)
		},
		{
			UDim2.new(0.12, 0, -0.20, 0),
			UDim2.new(0.70, 0, 0.62, 0)
		},
		{
			UDim2.new(-0.15, 0, 0.28, 0),
			UDim2.new(0.84, 0, 0.10, 0)
		},
		{
			UDim2.new(0.20, 0, 0.71, 0),
			UDim2.new(-0.08, 0, 0.67, 0)
		},
		{
			UDim2.new(0.56, 0, -0.17, 0),
			UDim2.new(0.23, 0, 0.78, 0)
		}
	}

	Orb1.Position = orbPositions[1][1]
	Orb2.Position = orbPositions[1][2]

	local function MoveOrbs()
		Window.OrbIndex += 1

		if Window.OrbIndex > #orbPositions then
			Window.OrbIndex = 1
		end

		local pair = orbPositions[Window.OrbIndex]

		Tween(Orb1, {
			Position = pair[1]
		}, 0.8)

		Tween(Orb2, {
			Position = pair[2]
		}, 0.95)
	end

	local TopBar = Create("Frame", {
		Parent = Shell,
		Position = UDim2.fromOffset(11, 11),
		Size = UDim2.new(1, -22, 0, TOPBAR_HEIGHT),
		BackgroundTransparency = 1,
		BorderSizePixel = 0,
		ZIndex = 20
	})

	local TopLine = Create("Frame", {
		Parent = TopBar,
		Position = UDim2.fromOffset(8, TOPBAR_HEIGHT - 1),
		Size = UDim2.new(1, -16, 0, 1),
		BackgroundColor3 = Theme.Border,
		BorderSizePixel = 0,
		ZIndex = 21
	})

	local LogoDot = Create("Frame", {
		Parent = TopBar,
		Position = UDim2.fromOffset(12, 22),
		Size = UDim2.fromOffset(10, 10),
		BackgroundColor3 = Theme.White,
		BorderSizePixel = 0,
		ZIndex = 22
	})

	Corner(LogoDot, 10)

	local Title = Label(
		TopBar,
		options.Title or "Axiom UI Library",
		15,
		Theme.Text,
		Enum.Font.GothamMedium
	)

	Title.Position = UDim2.fromOffset(36, 0)
	Title.Size = UDim2.fromOffset(200, TOPBAR_HEIGHT)
	Title.ZIndex = 22

	local Divider = Create("Frame", {
		Parent = TopBar,
		Position = UDim2.fromOffset(240, 17),
		Size = UDim2.fromOffset(1, 20),
		BackgroundColor3 = Theme.Border,
		BorderSizePixel = 0,
		ZIndex = 22
	})

	local Version = Label(
		TopBar,
		options.Version or "v4.1.0",
		10,
		Theme.SubText,
		Enum.Font.GothamMedium
	)

	Version.Position = UDim2.fromOffset(259, 0)
	Version.Size = UDim2.fromOffset(80, TOPBAR_HEIGHT)
	Version.ZIndex = 22

	local Close = TransparentButton(TopBar)
	Close.AnchorPoint = Vector2.new(1, 0.5)
	Close.Position = UDim2.new(1, -7, 0.5, 0)
	Close.Size = UDim2.fromOffset(28, 28)
	Close.ZIndex = 23

	local CloseText = Label(
		Close,
		"×",
		18,
		Theme.SubText,
		Enum.Font.Gotham
	)

	CloseText.Size = UDim2.fromScale(1, 1)
	CloseText.TextXAlignment = Enum.TextXAlignment.Center
	CloseText.ZIndex = 24

	Close.MouseEnter:Connect(function()
		Tween(CloseText, {
			TextColor3 = Theme.Danger
		}, 0.12)
	end)

	Close.MouseLeave:Connect(function()
		Tween(CloseText, {
			TextColor3 = Theme.SubText
		}, 0.12)
	end)

	Close.MouseButton1Click:Connect(function()
		ScreenGui:Destroy()
	end)

	local Sidebar = Create("Frame", {
		Parent = Shell,
		Position = UDim2.fromOffset(11, 65),
		Size = UDim2.new(0, SIDEBAR_WIDTH, 1, -76),
		BackgroundColor3 = Theme.Sidebar,
		BackgroundTransparency = 0,
		BorderSizePixel = 0,
		ZIndex = 20
	})

	Corner(Sidebar, 8)

	Sidebar.BackgroundColor3 = Color3.fromRGB(7, 7, 9)

	local SidebarDivider = Create("Frame", {
		Parent = Shell,
		Position = UDim2.fromOffset(
			11 + SIDEBAR_WIDTH,
			70
		),
		Size = UDim2.new(
			0,
			1,
			1,
			-86
		),
		BackgroundColor3 = Theme.Border,
		BorderSizePixel = 0,
		ZIndex = 21
	})

	local Brand = Label(
		Sidebar,
		"AXIOM",
		18,
		Theme.White,
		Enum.Font.GothamBlack
	)

	Brand.Position = UDim2.fromOffset(18, 16)
	Brand.Size = UDim2.fromOffset(150, 24)
	Brand.ZIndex = 22

	local BrandSub = Label(
		Sidebar,
		"UI LIBRARY",
		9,
		Theme.Muted,
		Enum.Font.GothamMedium
	)

	BrandSub.Position = UDim2.fromOffset(19, 36)
	BrandSub.Size = UDim2.fromOffset(120, 18)
	BrandSub.ZIndex = 22

	local TabHolder = Create("ScrollingFrame", {
		Parent = Sidebar,
		Position = UDim2.fromOffset(10, 62),
		Size = UDim2.new(1, -20, 1, -137),
		BackgroundTransparency = 1,
		BorderSizePixel = 0,
		ScrollBarThickness = 2,
		ScrollBarImageColor3 = Theme.White,
		CanvasSize = UDim2.fromOffset(0, 0),
		ScrollingDirection = Enum.ScrollingDirection.Y,
		ZIndex = 22
	})

	MakeList(TabHolder, TAB_GAP)

	local UserCard = Create("Frame", {
		Parent = Sidebar,
		AnchorPoint = Vector2.new(0.5, 1),
		Position = UDim2.new(0.5, 0, 1, -10),
		Size = UDim2.new(1, -18, 0, 63),
		BackgroundColor3 = Theme.Card,
		BorderSizePixel = 0,
		ZIndex = 22
	})

	Corner(UserCard, 8)
	Stroke(UserCard, Theme.Border, 0, 1)

	local Avatar = Create("ImageLabel", {
		Parent = UserCard,
		Position = UDim2.fromOffset(8, 8),
		Size = UDim2.fromOffset(38, 38),
		BackgroundColor3 = Theme.Background,
		BorderSizePixel = 0,
		Image = "rbxthumb://type=AvatarHeadShot&id=" ..
			tostring(LocalPlayer.UserId) ..
			"&w=150&h=150",
		ZIndex = 23
	})

	Corner(Avatar, 20)

	local PlayerName = Label(
		UserCard,
		LocalPlayer.DisplayName,
		10,
		Theme.Text,
		Enum.Font.GothamMedium
	)

	PlayerName.Position = UDim2.fromOffset(54, 8)
	PlayerName.Size = UDim2.new(1, -63, 0, 16)
	PlayerName.ZIndex = 23

	local Username = Label(
		UserCard,
		"@" .. LocalPlayer.Name,
		8,
		Theme.Muted
	)

	Username.Position = UDim2.fromOffset(54, 26)
	Username.Size = UDim2.new(1, -63, 0, 14)
	Username.ZIndex = 23

	local StatusDot = Create("Frame", {
		Parent = UserCard,
		Position = UDim2.fromOffset(9, 49),
		Size = UDim2.fromOffset(6, 6),
		BackgroundColor3 = Theme.Success,
		BorderSizePixel = 0,
		ZIndex = 23
	})

	Corner(StatusDot, 6)

	local Status = Label(
		UserCard,
		"Ready",
		8,
		Theme.Success,
		Enum.Font.GothamMedium
	)

	Status.Position = UDim2.fromOffset(20, 43)
	Status.Size = UDim2.fromOffset(60, 18)
	Status.ZIndex = 23

	local Content = Create("Frame", {
		Parent = Shell,
		Position = UDim2.fromOffset(
			11 + SIDEBAR_WIDTH + 1,
			65
		),
		Size = UDim2.new(
			1,
			-(SIDEBAR_WIDTH + 23),
			1,
			-76
		),
		BackgroundTransparency = 1,
		BorderSizePixel = 0,
		ZIndex = 20
	})

	local Dragging = false
	local DragStart
	local StartPosition

	TopBar.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 then
			Dragging = true
			DragStart = input.Position
			StartPosition = Shell.Position
		end
	end)

	TopBar.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 then
			Dragging = false
		end
	end)

	UserInputService.InputChanged:Connect(function(input)
		if Dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
			local delta = input.Position - DragStart

			Shell.Position = UDim2.new(
				StartPosition.X.Scale,
				StartPosition.X.Offset + delta.X,
				StartPosition.Y.Scale,
				StartPosition.Y.Offset + delta.Y
			)
		end
	end)

	function Window:SetVisible(value)
		Window.Visible = value
		Shell.Visible = value
	end

	function Window:SetTitle(value)
		Title.Text = tostring(value)
	end

	function Window:Notify(config)
		config = config or {}

		local holder = Create("Frame", {
			Parent = ScreenGui,
			AnchorPoint = Vector2.new(1, 1),
			Position = UDim2.new(1, 20, 1, -18),
			Size = UDim2.fromOffset(285, 72),
			BackgroundColor3 = Theme.Card,
			BorderSizePixel = 0,
			ZIndex = 200
		})

		Corner(holder, 10)
		Stroke(holder, Theme.Border, 0, 1)

		local accentColor =
			config.Type == "error"
				and Theme.Danger
				or config.Type == "warning"
					and Theme.Warning
					or Theme.White

		local accent = Create("Frame", {
			Parent = holder,
			Position = UDim2.fromOffset(0, 0),
			Size = UDim2.fromOffset(3, 72),
			BackgroundColor3 = accentColor,
			BorderSizePixel = 0,
			ZIndex = 201
		})

		Corner(accent, 3)

		local title = Label(
			holder,
			config.Title or "Axiom",
			11,
			Theme.Text,
			Enum.Font.GothamMedium
		)

		title.Position = UDim2.fromOffset(14, 8)
		title.Size = UDim2.new(1, -24, 0, 19)
		title.ZIndex = 201

		local content = Label(
			holder,
			config.Content or "",
			9,
			Theme.SubText
		)

		content.Position = UDim2.fromOffset(14, 28)
		content.Size = UDim2.new(1, -24, 0, 34)
		content.TextWrapped = true
		content.ZIndex = 201

		Tween(holder, {
			Position = UDim2.new(1, -18, 1, -18)
		}, 0.2)

		task.delay(config.Duration or 3, function()
			if holder.Parent then
				Tween(holder, {
					Position = UDim2.new(1, 20, 1, -18)
				}, 0.18)

				task.delay(0.2, function()
					holder:Destroy()
				end)
			end
		end)

		return holder
	end

	function Window:AddTabs(name)
		assert(type(name) == "string", "Tab name must be a string")

		if Window.Tabs[name] then
			return Window.Tabs[name]
		end

		local Tab = {
			Name = name,
			Sections = {}
		}

		local TabButton = Button(TabHolder)

		TabButton.Size = UDim2.new(1, 0, 0, TAB_HEIGHT)
		TabButton.LayoutOrder = #Window.TabOrder + 1
		TabButton.BackgroundTransparency = 1
		TabButton.ZIndex = 24

		Corner(TabButton, 7)

		local Indicator = Create("Frame", {
			Parent = TabButton,
			Position = UDim2.new(0, 0, 0.5, -8),
			Size = UDim2.fromOffset(3, 16),
			BackgroundColor3 = Theme.White,
			BackgroundTransparency = 1,
			BorderSizePixel = 0,
			ZIndex = 25
		})

		Corner(Indicator, 3)

		local Icon = Label(
			TabButton,
			"◆",
			7,
			Theme.Muted,
			Enum.Font.GothamBold
		)

		Icon.Position = UDim2.fromOffset(8, 0)
		Icon.Size = UDim2.fromOffset(22, TAB_HEIGHT)
		Icon.TextXAlignment = Enum.TextXAlignment.Center
		Icon.ZIndex = 25

		local TabText = Label(
			TabButton,
			name,
			11,
			Theme.SubText,
			Enum.Font.GothamMedium
		)

		TabText.Position = UDim2.fromOffset(38, 0)
		TabText.Size = UDim2.new(1, -43, 1, 0)
		TabText.ZIndex = 25

		local Page = Create("ScrollingFrame", {
			Parent = Content,
			Position = UDim2.fromOffset(14, 12),
			Size = UDim2.new(1, -28, 1, -24),
			BackgroundTransparency = 1,
			BorderSizePixel = 0,
			Visible = false,
			ScrollBarThickness = 2,
			ScrollBarImageColor3 = Theme.White,
			CanvasSize = UDim2.fromOffset(0, 0),
			ScrollingDirection = Enum.ScrollingDirection.Y,
			ZIndex = 26
		})

		local PageLayout = MakeList(Page, SECTION_GAP)

		PageLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
			Page.CanvasSize = UDim2.fromOffset(
				0,
				PageLayout.AbsoluteContentSize.Y + 4
			)
		end)

		function Tab:Activate()
			for _, other in ipairs(Window.TabOrder) do
				if other ~= Tab then
					other.Page.Visible = false

					Tween(other.TabButton, {
						BackgroundTransparency = 1
					}, 0.1)

					Tween(other.TabText, {
						TextColor3 = Theme.SubText
					}, 0.1)

					Tween(other.Icon, {
						TextColor3 = Theme.Muted
					}, 0.1)

					Tween(other.Indicator, {
						BackgroundTransparency = 1
					}, 0.1)
				end
			end

			Page.Visible = true

			Tween(TabButton, {
				BackgroundTransparency = 0,
				BackgroundColor3 = Theme.CardHover
			}, 0.12)

			Tween(TabText, {
				TextColor3 = Theme.White
			}, 0.12)

			Tween(Icon, {
				TextColor3 = Theme.White
			}, 0.12)

			Tween(Indicator, {
				BackgroundTransparency = 0
			}, 0.12)

			Window.CurrentTab = Tab
			MoveOrbs()
		end

		TabButton.MouseEnter:Connect(function()
			if Window.CurrentTab ~= Tab then
				Tween(TabButton, {
					BackgroundTransparency = 0,
					BackgroundColor3 = Theme.Card
				}, 0.1)
			end
		end)

		TabButton.MouseLeave:Connect(function()
			if Window.CurrentTab ~= Tab then
				Tween(TabButton, {
					BackgroundTransparency = 1
				}, 0.1)
			end
		end)

		TabButton.MouseButton1Click:Connect(function()
			Tab:Activate()
		end)

		function Tab:AddSection(name)
			local Section = {
				Elements = {}
			}

			local Frame = Create("Frame", {
				Parent = Page,
				Size = UDim2.new(1, 0, 0, 0),
				AutomaticSize = Enum.AutomaticSize.Y,
				BackgroundColor3 = Theme.Card,
				BorderSizePixel = 0,
				ZIndex = 27
			})

			Corner(Frame, 9)
			Stroke(Frame, Theme.Border, 0, 1)

			local Header = Create("Frame", {
				Parent = Frame,
				Size = UDim2.new(1, 0, 0, 36),
				BackgroundTransparency = 1,
				ZIndex = 28
			})

			local HeaderText = Label(
				Header,
				name,
				11,
				Theme.Text,
				Enum.Font.GothamMedium
			)

			HeaderText.Position = UDim2.fromOffset(12, 0)
			HeaderText.Size = UDim2.new(1, -24, 1, 0)
			HeaderText.ZIndex = 29

			Create("Frame", {
				Parent = Frame,
				Position = UDim2.fromOffset(12, 35),
				Size = UDim2.new(1, -24, 0, 1),
				BackgroundColor3 = Theme.Border,
				BorderSizePixel = 0,
				ZIndex = 29
			})

			local Container = Create("Frame", {
				Parent = Frame,
				Position = UDim2.fromOffset(10, 42),
				Size = UDim2.new(1, -20, 0, 0),
				AutomaticSize = Enum.AutomaticSize.Y,
				BackgroundTransparency = 1,
				ZIndex = 28
			})

			MakeList(Container, CONTROL_GAP)

			local function Store(element)
				table.insert(Section.Elements, element)
				return element
			end

			function Section:AddToggle(config)
				config = config or {}

				local Toggle = {
					Value = config.Default or false
				}

				local Row = Button(Container)

				Row.Size = UDim2.new(1, 0, 0, 45)
				Row.BackgroundColor3 = Theme.Background2
				Row.BackgroundTransparency = 0
				Row.ZIndex = 29

				Corner(Row, 7)
				Stroke(Row, Theme.Border, 0, 1)

				local NameText = Label(
					Row,
					config.Name or "Toggle",
					11,
					Theme.Text,
					Enum.Font.GothamMedium
				)

				NameText.Position = UDim2.fromOffset(11, 4)
				NameText.Size = UDim2.new(1, -90, 0, 17)
				NameText.ZIndex = 30

				local Description = Label(
					Row,
					config.Description or "",
					9,
					Theme.Muted
				)

				Description.Position = UDim2.fromOffset(11, 23)
				Description.Size = UDim2.new(1, -90, 0, 14)
				Description.ZIndex = 30

				local Switch = Create("Frame", {
					Parent = Row,
					AnchorPoint = Vector2.new(1, 0.5),
					Position = UDim2.new(1, -10, 0.5, 0),
					Size = UDim2.fromOffset(39, 20),
					BackgroundColor3 = Theme.Background,
					BorderSizePixel = 0,
					ZIndex = 30
				})

				Corner(Switch, 10)
				Stroke(Switch, Theme.Border, 0, 1)

				local Knob = Create("Frame", {
					Parent = Switch,
					Position = UDim2.new(0, 3, 0.5, -7),
					Size = UDim2.fromOffset(14, 14),
					BackgroundColor3 = Theme.Muted,
					BorderSizePixel = 0,
					ZIndex = 31
				})

				Corner(Knob, 7)

				function Toggle:Set(value)
					Toggle.Value = value

					if value then
						Tween(Switch, {
							BackgroundColor3 = Theme.White
						}, 0.12)

						Tween(Knob, {
							Position = UDim2.new(1, -17, 0.5, -7),
							BackgroundColor3 = Theme.Black
						}, 0.12)
					else
						Tween(Switch, {
							BackgroundColor3 = Theme.Background
						}, 0.12)

						Tween(Knob, {
							Position = UDim2.new(0, 3, 0.5, -7),
							BackgroundColor3 = Theme.Muted
						}, 0.12)
					end

					Callback(config.Callback, value)
				end

				Row.MouseButton1Click:Connect(function()
					Toggle:Set(not Toggle.Value)
				end)

				Toggle:Set(Toggle.Value)

				return Store(Toggle)
			end

			function Section:AddButton(config)
				config = config or {}

				local object = Button(Container)

				object.Size = UDim2.new(1, 0, 0, 38)
				object.BackgroundColor3 = Theme.White
				object.BackgroundTransparency = 0
				object.ZIndex = 29

				Corner(object, 7)

				local text = Label(
					object,
					config.Name or "Execute",
					11,
					Theme.Black,
					Enum.Font.GothamMedium
				)

				text.Size = UDim2.fromScale(1, 1)
				text.TextXAlignment = Enum.TextXAlignment.Center
				text.ZIndex = 30

				object.MouseEnter:Connect(function()
					Tween(object, {
						BackgroundColor3 = Theme.Text
					}, 0.1)
				end)

				object.MouseLeave:Connect(function()
					Tween(object, {
						BackgroundColor3 = Theme.White
					}, 0.1)
				end)

				object.MouseButton1Click:Connect(function()
					Callback(config.Callback)
				end)

				return Store(object)
			end

			function Section:AddDropdown(config)
				config = config or {}

				local Dropdown = {
					Value = config.Default or (config.Options and config.Options[1])
				}

				local open = false
				local optionCount = #(config.Options or {})

				local Holder = Create("Frame", {
					Parent = Container,
					Size = UDim2.new(1, 0, 0, 38),
					BackgroundColor3 = Theme.Background2,
					BorderSizePixel = 0,
					ClipsDescendants = true,
					ZIndex = 29
				})

				Corner(Holder, 7)
				Stroke(Holder, Theme.Border, 0, 1)

				local Select = TransparentButton(Holder)

				Select.Size = UDim2.new(1, 0, 0, 38)
				Select.ZIndex = 30

				local NameText = Label(
					Select,
					config.Name or "Dropdown",
					11,
					Theme.Text,
					Enum.Font.GothamMedium
				)

				NameText.Position = UDim2.fromOffset(11, 0)
				NameText.Size = UDim2.new(0.45, 0, 1, 0)
				NameText.ZIndex = 31

				local Current = Label(
					Select,
					tostring(Dropdown.Value or "Select"),
					10,
					Theme.SubText
				)

				Current.AnchorPoint = Vector2.new(1, 0)
				Current.Position = UDim2.new(1, -28, 0, 0)
				Current.Size = UDim2.new(0.42, 0, 1, 0)
				Current.TextXAlignment = Enum.TextXAlignment.Right
				Current.ZIndex = 31

				local Arrow = Label(
					Select,
					"⌄",
					12,
					Theme.White
				)

				Arrow.AnchorPoint = Vector2.new(1, 0.5)
				Arrow.Position = UDim2.new(1, -7, 0.5, 0)
				Arrow.Size = UDim2.fromOffset(12, 16)
				Arrow.TextXAlignment = Enum.TextXAlignment.Center
				Arrow.ZIndex = 31

				local List = Create("ScrollingFrame", {
					Parent = Holder,
					Position = UDim2.fromOffset(7, 43),
					Size = UDim2.new(1, -14, 0, 105),
					BackgroundTransparency = 1,
					BorderSizePixel = 0,
					ScrollBarThickness = 2,
					ScrollBarImageColor3 = Theme.White,
					ZIndex = 31
				})

				MakeList(List, 3)

				for _, option in ipairs(config.Options or {}) do
					local optionButton = Button(List)

					optionButton.Size = UDim2.new(1, -2, 0, 30)
					optionButton.BackgroundColor3 = Theme.Card
					optionButton.BackgroundTransparency = 0
					optionButton.ZIndex = 32

					Corner(optionButton, 6)

					local optionText = Label(
						optionButton,
						tostring(option),
						10,
						Theme.SubText
					)

					optionText.Position = UDim2.fromOffset(9, 0)
					optionText.Size = UDim2.new(1, -18, 1, 0)
					optionText.ZIndex = 33

					optionButton.MouseEnter:Connect(function()
						Tween(optionButton, {
							BackgroundColor3 = Theme.White
						}, 0.1)

						Tween(optionText, {
							TextColor3 = Theme.Black
						}, 0.1)
					end)

					optionButton.MouseLeave:Connect(function()
						Tween(optionButton, {
							BackgroundColor3 = Theme.Card
						}, 0.1)

						Tween(optionText, {
							TextColor3 = Theme.SubText
						}, 0.1)
					end)

					optionButton.MouseButton1Click:Connect(function()
						Dropdown.Value = option
						Current.Text = tostring(option)
						open = false

						Tween(Holder, {
							Size = UDim2.new(1, 0, 0, 38)
						}, 0.15)

						Arrow.Rotation = 0

						Callback(config.Callback, option)
					end)
				end

				Select.MouseButton1Click:Connect(function()
					open = not open

					if open then
						local visible = math.min(optionCount, 3)

						Tween(Holder, {
							Size = UDim2.new(
								1,
								0,
								0,
								43 + visible * 33
							)
						}, 0.16)

						Arrow.Rotation = 180
					else
						Tween(Holder, {
							Size = UDim2.new(1, 0, 0, 38)
						}, 0.16)

						Arrow.Rotation = 0
					end
				end)

				function Dropdown:Set(value)
					Dropdown.Value = value
					Current.Text = tostring(value)
					Callback(config.Callback, value)
				end

				return Store(Dropdown)
			end

			function Section:AddMultiDropdown(config)
				config = config or {}

				local Dropdown = {
					Values = {}
				}

				local open = false
				local optionButtons = {}

				local Holder = Create("Frame", {
					Parent = Container,
					Size = UDim2.new(1, 0, 0, 38),
					BackgroundColor3 = Theme.Background2,
					BorderSizePixel = 0,
					ClipsDescendants = true,
					ZIndex = 29
				})

				Corner(Holder, 7)
				Stroke(Holder, Theme.Border, 0, 1)

				local Select = TransparentButton(Holder)
				Select.Size = UDim2.new(1, 0, 0, 38)
				Select.ZIndex = 30

				local NameText = Label(
					Select,
					config.Name or "Multi Dropdown",
					11,
					Theme.Text,
					Enum.Font.GothamMedium
				)

				NameText.Position = UDim2.fromOffset(11, 0)
				NameText.Size = UDim2.new(0.45, 0, 1, 0)
				NameText.ZIndex = 31

				local Current = Label(
					Select,
					"None",
					10,
					Theme.SubText
				)

				Current.AnchorPoint = Vector2.new(1, 0)
				Current.Position = UDim2.new(1, -28, 0, 0)
				Current.Size = UDim2.new(0.42, 0, 1, 0)
				Current.TextXAlignment = Enum.TextXAlignment.Right
				Current.ZIndex = 31

				local Arrow = Label(
					Select,
					"⌄",
					12,
					Theme.White
				)

				Arrow.AnchorPoint = Vector2.new(1, 0.5)
				Arrow.Position = UDim2.new(1, -7, 0.5, 0)
				Arrow.Size = UDim2.fromOffset(12, 16)
				Arrow.TextXAlignment = Enum.TextXAlignment.Center
				Arrow.ZIndex = 31

				local List = Create("ScrollingFrame", {
					Parent = Holder,
					Position = UDim2.fromOffset(7, 43),
					Size = UDim2.new(1, -14, 0, 105),
					BackgroundTransparency = 1,
					BorderSizePixel = 0,
					ScrollBarThickness = 2,
					ScrollBarImageColor3 = Theme.White,
					ZIndex = 31
				})

				MakeList(List, 3)

				local function Refresh()
					local values = {}

					for value in pairs(Dropdown.Values) do
						table.insert(values, tostring(value))
					end

					table.sort(values)

					Current.Text = #values > 0 and table.concat(values, ", ") or "None"
				end

				for _, option in ipairs(config.Options or {}) do
					local optionButton = Button(List)

					optionButton.Size = UDim2.new(1, -2, 0, 30)
					optionButton.BackgroundColor3 = Theme.Card
					optionButton.BackgroundTransparency = 0
					optionButton.ZIndex = 32

					Corner(optionButton, 6)

					local optionText = Label(
						optionButton,
						tostring(option),
						10,
						Theme.SubText
					)

					optionText.Position = UDim2.fromOffset(9, 0)
					optionText.Size = UDim2.new(1, -18, 1, 0)
					optionText.ZIndex = 33

					optionButtons[option] = {
						Button = optionButton,
						Text = optionText
					}

					optionButton.MouseButton1Click:Connect(function()
						Dropdown.Values[option] = not Dropdown.Values[option]

						local selected = Dropdown.Values[option]

						Tween(optionButton, {
							BackgroundColor3 = selected and Theme.White or Theme.Card
						}, 0.1)

						Tween(optionText, {
							TextColor3 = selected and Theme.Black or Theme.SubText
						}, 0.1)

						Refresh()
						Callback(config.Callback, Dropdown.Values)
					end)
				end

				Select.MouseButton1Click:Connect(function()
					open = not open

					if open then
						local visible = math.min(#(config.Options or {}), 3)

						Tween(Holder, {
							Size = UDim2.new(
								1,
								0,
								0,
								43 + visible * 33
							)
						}, 0.16)

						Arrow.Rotation = 180
					else
						Tween(Holder, {
							Size = UDim2.new(1, 0, 0, 38)
						}, 0.16)

						Arrow.Rotation = 0
					end
				end)

				function Dropdown:Set(values)
					Dropdown.Values = {}

					for _, value in ipairs(values or {}) do
						Dropdown.Values[value] = true

						if optionButtons[value] then
							Tween(optionButtons[value].Button, {
								BackgroundColor3 = Theme.White
							}, 0.1)

							Tween(optionButtons[value].Text, {
								TextColor3 = Theme.Black
							}, 0.1)
						end
					end

					Refresh()
					Callback(config.Callback, Dropdown.Values)
				end

				return Store(Dropdown)
			end

			function Section:AddSlider(config)
				config = config or {}

				local min = config.Min or 0
				local max = config.Max or 100

				local Slider = {
					Value = math.clamp(
						config.Default or min,
						min,
						max
					)
				}

				local dragging = false

				local Holder = Create("Frame", {
					Parent = Container,
					Size = UDim2.new(1, 0, 0, 54),
					BackgroundColor3 = Theme.Background2,
					BorderSizePixel = 0,
					ZIndex = 29
				})

				Corner(Holder, 7)
				Stroke(Holder, Theme.Border, 0, 1)

				local NameText = Label(
					Holder,
					config.Name or "Slider",
					11,
					Theme.Text,
					Enum.Font.GothamMedium
				)

				NameText.Position = UDim2.fromOffset(11, 4)
				NameText.Size = UDim2.new(0.6, 0, 0, 16)
				NameText.ZIndex = 30

				local ValueText = Label(
					Holder,
					tostring(Slider.Value),
					10,
					Theme.SubText
				)

				ValueText.AnchorPoint = Vector2.new(1, 0)
				ValueText.Position = UDim2.new(1, -11, 0, 4)
				ValueText.Size = UDim2.fromOffset(65, 16)
				ValueText.TextXAlignment = Enum.TextXAlignment.Right
				ValueText.ZIndex = 30

				local Bar = Create("Frame", {
					Parent = Holder,
					Position = UDim2.fromOffset(11, 34),
					Size = UDim2.new(1, -22, 0, 4),
					BackgroundColor3 = Theme.Border,
					BorderSizePixel = 0,
					ZIndex = 30
				})

				Corner(Bar, 4)

				local percent = (Slider.Value - min) / math.max(1, max - min)

				local Fill = Create("Frame", {
					Parent = Bar,
					Size = UDim2.new(percent, 0, 1, 0),
					BackgroundColor3 = Theme.White,
					BorderSizePixel = 0,
					ZIndex = 31
				})

				Corner(Fill, 4)

				local Knob = Create("Frame", {
					Parent = Bar,
					AnchorPoint = Vector2.new(0.5, 0.5),
					Position = UDim2.new(percent, 0, 0.5, 0),
					Size = UDim2.fromOffset(10, 10),
					BackgroundColor3 = Theme.White,
					BorderSizePixel = 0,
					ZIndex = 32
				})

				Corner(Knob, 8)

				local function SetFromX(x)
					local p = math.clamp(
						(x - Bar.AbsolutePosition.X) / Bar.AbsoluteSize.X,
						0,
						1
					)

					local value = min + (max - min) * p

					if config.Increment then
						value =
							math.floor(
								value / config.Increment + 0.5
							) * config.Increment
					else
						value = math.floor(value + 0.5)
					end

					value = math.clamp(value, min, max)

					Slider.Value = value
					ValueText.Text = tostring(value)

					Tween(Fill, {
						Size = UDim2.new(
							(value - min) / math.max(1, max - min),
							0,
							1,
							0
						)
					}, 0.05)

					Tween(Knob, {
						Position = UDim2.new(
							(value - min) / math.max(1, max - min),
							0,
							0.5,
							0
						)
					}, 0.05)

					Callback(config.Callback, value)
				end

				Bar.InputBegan:Connect(function(input)
					if input.UserInputType == Enum.UserInputType.MouseButton1 then
						dragging = true
						SetFromX(input.Position.X)
					end
				end)

				UserInputService.InputChanged:Connect(function(input)
					if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
						SetFromX(input.Position.X)
					end
				end)

				UserInputService.InputEnded:Connect(function(input)
					if input.UserInputType == Enum.UserInputType.MouseButton1 then
						dragging = false
					end
				end)

				function Slider:Set(value)
					value = math.clamp(value, min, max)

					SetFromX(
						Bar.AbsolutePosition.X +
						Bar.AbsoluteSize.X *
						((value - min) / math.max(1, max - min))
					)
				end

				return Store(Slider)
			end

			function Section:AddTextbox(config)
				config = config or {}

				local Holder = Create("Frame", {
					Parent = Container,
					Size = UDim2.new(1, 0, 0, 41),
					BackgroundColor3 = Theme.Background2,
					BorderSizePixel = 0,
					ZIndex = 29
				})

				Corner(Holder, 7)
				Stroke(Holder, Theme.Border, 0, 1)

				local NameText = Label(
					Holder,
					config.Name or "Textbox",
					11,
					Theme.Text,
					Enum.Font.GothamMedium
				)

				NameText.Position = UDim2.fromOffset(11, 0)
				NameText.Size = UDim2.new(0.32, 0, 1, 0)
				NameText.ZIndex = 30

				local Input = Create("TextBox", {
					Parent = Holder,
					AnchorPoint = Vector2.new(1, 0.5),
					Position = UDim2.new(1, -8, 0.5, 0),
					Size = UDim2.new(0.58, 0, 0, 27),
					BackgroundColor3 = Theme.Card,
					BorderSizePixel = 0,
					ClearTextOnFocus = false,
					Text = config.Default or "",
					PlaceholderText = config.Placeholder or "Enter text...",
					PlaceholderColor3 = Theme.Muted,
					TextColor3 = Theme.Text,
					TextSize = 10,
					Font = Enum.Font.Gotham,
					TextXAlignment = Enum.TextXAlignment.Left,
					ZIndex = 30
				})

				Corner(Input, 6)

				local Padding = Instance.new("UIPadding")
				Padding.PaddingLeft = UDim.new(0, 8)
				Padding.PaddingRight = UDim.new(0, 8)
				Padding.Parent = Input

				Input.FocusLost:Connect(function()
					Callback(config.Callback, Input.Text)
				end)

				return Store(Input)
			end

			function Section:AddKeybind(config)
				config = config or {}

				local Keybind = {
					Key = config.Key or Enum.KeyCode.RightControl
				}

				local listening = false

				local Row = Create("Frame", {
					Parent = Container,
					Size = UDim2.new(1, 0, 0, 41),
					BackgroundColor3 = Theme.Background2,
					BorderSizePixel = 0,
					ZIndex = 29
				})

				Corner(Row, 7)
				Stroke(Row, Theme.Border, 0, 1)

				local NameText = Label(
					Row,
					config.Name or "Keybind",
					11,
					Theme.Text,
					Enum.Font.GothamMedium
				)

				NameText.Position = UDim2.fromOffset(11, 0)
				NameText.Size = UDim2.new(0.45, 0, 1, 0)
				NameText.ZIndex = 30

				local KeyButton = Button(Row)
				KeyButton.AnchorPoint = Vector2.new(1, 0.5)
				KeyButton.Position = UDim2.new(1, -8, 0.5, 0)
				KeyButton.Size = UDim2.fromOffset(100, 27)
				KeyButton.BackgroundColor3 = Theme.Card
				KeyButton.BackgroundTransparency = 0
				KeyButton.ZIndex = 30

				Corner(KeyButton, 6)
				Stroke(KeyButton, Theme.Border, 0, 1)

				local KeyText = Label(
					KeyButton,
					Keybind.Key.Name,
					10,
					Theme.SubText,
					Enum.Font.GothamMedium
				)

				KeyText.Size = UDim2.fromScale(1, 1)
				KeyText.TextXAlignment = Enum.TextXAlignment.Center
				KeyText.ZIndex = 31

				KeyButton.MouseButton1Click:Connect(function()
					if listening then
						return
					end

					listening = true
					KeyText.Text = "PRESS KEY"
					KeyText.TextColor3 = Theme.White
				end)

				UserInputService.InputBegan:Connect(function(input, processed)
					if processed then
						return
					end

					if listening then
						if input.KeyCode ~= Enum.KeyCode.Unknown then
							Keybind.Key = input.KeyCode
							KeyText.Text = input.KeyCode.Name
							KeyText.TextColor3 = Theme.SubText
							listening = false
						end

						return
					end

					if input.KeyCode == Keybind.Key then
						Callback(config.Callback)
					end
				end)

				function Keybind:Set(key)
					Keybind.Key = key
					KeyText.Text = key.Name
				end

				return Store(Keybind)
			end

			function Section:AddLabel(text)
				local object = Label(
					Container,
					text or "",
					10,
					Theme.Muted
				)

				object.Size = UDim2.new(1, 0, 0, 20)
				object.ZIndex = 30

				return Store(object)
			end

			function Section:AddParagraph(title, text)
				local Holder = Create("Frame", {
					Parent = Container,
					Size = UDim2.new(1, 0, 0, 57),
					BackgroundColor3 = Theme.Background2,
					BorderSizePixel = 0,
					ZIndex = 29
				})

				Corner(Holder, 7)
				Stroke(Holder, Theme.Border, 0, 1)

				local Title = Label(
					Holder,
					title or "Paragraph",
					11,
					Theme.Text,
					Enum.Font.GothamMedium
				)

				Title.Position = UDim2.fromOffset(11, 5)
				Title.Size = UDim2.new(1, -22, 0, 16)
				Title.ZIndex = 30

				local ContentText = Label(
					Holder,
					text or "",
					9,
					Theme.Muted
				)

				ContentText.Position = UDim2.fromOffset(11, 22)
				ContentText.Size = UDim2.new(1, -22, 0, 29)
				ContentText.TextWrapped = true
				ContentText.ZIndex = 30

				return Store(Holder)
			end

			function Section:AddDivider()
				local Divider = Create("Frame", {
					Parent = Container,
					Size = UDim2.new(1, 0, 0, 1),
					BackgroundColor3 = Theme.Border,
					BorderSizePixel = 0,
					ZIndex = 30
				})

				return Store(Divider)
			end

			table.insert(Tab.Sections, Section)

			return Section
		end

		Tab.TabButton = TabButton
		Tab.Page = Page
		Tab.Icon = Icon
		Tab.TabText = TabText
		Tab.Indicator = Indicator

		Window.Tabs[name] = Tab
		table.insert(Window.TabOrder, Tab)

		local tabHeight =
			#Window.TabOrder * TAB_HEIGHT +
			math.max(0, #Window.TabOrder - 1) * TAB_GAP

		TabHolder.CanvasSize = UDim2.fromOffset(
			0,
			tabHeight
		)

		if not Window.CurrentTab then
			task.defer(function()
				Tab:Activate()
			end)
		end

		return Tab
	end

	Window.AddTab = Window.AddTabs

	Shell.Size = UDim2.fromOffset(850, 525)
	Shell.BackgroundTransparency = 0

	Tween(Shell, {
		Size = UDim2.fromOffset(
			WINDOW_WIDTH,
			WINDOW_HEIGHT
		)
	}, 0.3)

	return Window
end

return Axiom
